# configuration for git
[hub]
        protocol = https
[user]
        name = lightski
        email = drleitzke@gmail.com
[credential]
        helper = cache
[credential "https://github.com"]
    username = lightski
[credential "https://bitbucket.com"]
    username = lightski
[alias]
        co = checkout
        promote = !$ZSH/bin/git-promote
        wtf     = !$ZSH/bin/git-wtf
        rank-contributors = !$ZSH/bin/git-rank-contributors
        count   = !git shortlog -sn
[color]
        diff = auto
        status = auto
        branch = auto
        ui = true
[core]
        excludesfile = ~/.gitignore
        editor = vim
[apply]
        whitespace = nowarn
[mergetool]
        keepBackup = false
[difftool]
        prompt = false
[help]
        autocorrect = 1
[push]
        default = simple
